# Transfermarkt Pipeline (Prefect)

## Requirements

- docker
- docker-compose

## How to run

minimal services:
```
docker compose up -d prefect-agent redis
```

all services:
```
docker compose up -d
```
